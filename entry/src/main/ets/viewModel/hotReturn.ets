export default class hot_return {
  hot_song_id: string;
  album_id: string;

  constructor(hot_song_id: string, album_id: string) {
    this.hot_song_id = hot_song_id;
    this.album_id = album_id;
  }
}